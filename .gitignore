node_modules/
dist/
# The Tesseract model is bundled by the installer but never committed (large
# binary); the directory itself is tracked so the README's download step works
# on a fresh clone and so electron-builder's extraResources path always exists.
ocr-data/*
!ocr-data/.gitkeep
